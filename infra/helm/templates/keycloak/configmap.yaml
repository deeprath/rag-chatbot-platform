apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rag-chatbot.componentFullname" (list $ "keycloak-realm") }}
  labels:
    {{- include "rag-chatbot.labels" $ | nindent 4 }}
data:
  realm-export.json: |-
    {{- $.Files.Get "files/realm-export.json" | nindent 4 }}
