apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-chatbot.componentFullname" (list $ "minio") }}
  labels:
    {{- include "rag-chatbot.labels" $ | nindent 4 }}
    {{- include "rag-chatbot.selectorLabels" (list $ "minio") | nindent 4 }}
spec:
  selector:
    {{- include "rag-chatbot.selectorLabels" (list $ "minio") | nindent 4 }}
  ports:
    - port: 9000
      targetPort: api
      name: api
    - port: 9001
      targetPort: console
      name: console
